package backend;

// this essentially imports classes i want to be accessible that are not used anywhere else.

import shaders.AdjustColorShader;
import shaders.AdjustColorScreenspace;
import shaders.OverlayShader;
import shaders.WiggleEffectRuntime;

import backend.QuickMemBank;

class Preserve {
    // we literally do nothing
}